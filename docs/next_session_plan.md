# 세션 실행표 — 2026-08-27 밤 → 08-28

작성 2026-08-27 22:30. 직전 세션은 [`hardware_test_20260827.md`](hardware_test_20260827.md) ·
[`_sensing.md`](hardware_test_20260827_sensing.md) 다.
**세션이 끝나면 결과는 `hardware_test_20260828.md` 로 가고 이 문서는 그다음 세션용으로
다시 쓴다** — 항상 "바로 다음에 할 일" 하나만 담는다.

## 오늘 할 일 — 한 줄

**08-26 의 제조사 표를 인자 그대로 한 번 더 돌리고**(§4), 방향 빚 20 분을 갚고(§6),
**DMM 직렬 전류 교정(#4)으로 08-09 부터 열려 있던 절대 교정을 끝낸다**(§7).

## 넘어온 상황

| | |
|---|---|
| 배터리 | **28.567 V** — 충전 불필요. 아래 예산 3.8 Ah 를 빼도 약 25.8 V |
| 리그 | 바퀴·벨트 장착, 띄움. ⚠ **id1 벨트 텐션이 08-26 이후 변경됨** — 08-26 대비 id1 비교는 교락 (#44) |
| 열 상태 | 마지막 구동 08-27 04:46. **17 시간 이상 정지 — 진짜 냉간이다** |
| 브레이크인 | **90 / 110 분.** CW 55 / CCW 35 — 빚 그대로 (#32) |
| 참 0 A | 확보 — `gp27 2033.486` / `gp28 2031.270` (08-27 03:38, 냉간) |
| 직전 세션 | 브레이크인 **0 분**. 기준선·계측 전용이었다 |

---

## ⚠ 순서가 결과를 좌우한다

```
1. preflight                     (읽기 전용)
2. 냉간 영점 쌍  OFF→ON           (읽기 전용 — 모터를 안 돌리므로 3 을 안 깨뜨린다)
3. 냉간 기준선  cold*b            ← 반드시 첫 구동. 한 번이라도 돌리면 냉간이 아니다
4. 제조사 표 1 회  bi1b~bi4b      ← 08-26 인자 그대로
5. 사후 기준선  post*b            ← ⚠ bi4b 종료 20 분 안에
6. 방향 빚  CCW 20 분  bi4ccw
7. ★ DMM 직렬 전류 교정 (#4)      ← 배선 변경이므로 맨 뒤
```

**5 를 6 앞에 두는 이유**: 사후 기준선은 "표 1 회의 순효과"를 재는 것이라 **표가 끝난 직후의
열 상태**에서 잡아야 08-26 `post*` 와 대조된다. 빚 20 분을 먼저 끼우면 그 창을 놓친다.

**7 을 맨 뒤에 두는 이유**: 급전선을 끊는 것은 배선 변경이다(#40). 기구 측정이 다 끝난 뒤에
해야 오늘 §3~§6 이 한 배선 상태 안에서 나온다.

⚠ **`preflight.py` 를 §2 의 영점 캡처로 쓰면 안 된다** — 그건 `Z` 를 보낸다 (`:183`).
계통 점검용으로만 쓰고, 영점은 `volt_compare.py` 로 잡는다 (조치 #46).

---

## 1. 계통 점검 — 1 분

```bash
python3 test/preflight.py --sec 20
```

⚠ `gp27`/`gp28` 정지 raw 주의가 **2 건 뜬다.** `REST_REF` 가 8/11·8/12 값이라 그렇고,
08-27 §4 에서 그 간극이 드리프트가 아니라 **배선 변경 계단**임을 확인했다. 진행 가능 —
조치 #42 로 정리 대기 중이다.

**DMM 으로 배터리 전압을 읽고 기록할 것.** §3 에서 `--dmm` 으로 로그에 박으면 분압비
확인점이 하나 더 쌓인다 (08-27 의 28.59 V 점이 `D ∈ [11.128, 11.133]` 을 줬다).

---

## 2. 냉간 영점 쌍 — 읽기 전용, 3 분

**목적**: ① 참 0 A 의 하루 단위 재현성(#27), ② 대기전류 스텝의 재현성(#45).

```bash
# 모터 스위치 OFF (파이·Pico 는 켜 둔다)
python3 test/volt_compare.py --sec 60 --rate 50 -o test/logs/zero_off_cold_0828.csv
# 모터 스위치 ON — 3 분 안에
python3 test/volt_compare.py --sec 60 --rate 50 -o test/logs/zero_on_cold_0828.csv
```

⚠ **두 캡처를 붙여서 뜰 것.** 08-26 은 1 시간을 벌려 놓아 귀속이 불가능했다.

| 08-27 값 (대조용) | gp27 | gp28 |
|---|---:|---:|
| OFF (참 0 A) | 2033.486 | 2031.270 |
| ON (정지) | 2042.930 | 2040.697 |
| **스텝** | **+9.4438** | **+9.4265** |

⚠ OFF 에서 GP26 은 죽고(부유 노드) MD400 은 무응답이다 — **정상**이다.
⚠ **Δ(핀 33)를 스위치 OFF 로 재지 말 것.** 접지 귀환 전류가 없어져 값이 달라진다.

---

## 3. 냉간 기준선 — 15 분, 0.10 Ah

**목적**: 오늘 표의 "이전" 기준선. 동시에 08-27 `cold*` 와 짝지어 **하루를 건너뛴 절대 전력
반복성**(#43)을 잰다.

```bash
python3 test/breakin.py --tag cold1b --id 1 --cycles 1 --speeds 300,600,900,1500 \
    --dwell 15 --rest 10 --zero-sec 20 --dmm <DMM> --vmin 22.5
python3 test/breakin.py --tag cold2b --id 2 --cycles 1 --speeds 300,600,900,1500 \
    --dwell 15 --rest 10 --zero-sec 20 --vmin 22.5
python3 test/breakin.py --tag coldpb --mirror --cycles 2 --speeds 300,600,900,1500 \
    --dwell 15 --rest 10 --zero-sec 20 --vmin 22.5
```

**08-27 `cold*` 와 인자가 완전히 같다.** 리그도 그대로다 — 달라지는 건 하루뿐이다.

| 08-27 냉간 값 (대조용) | |
|---|---|
| `cold1` P 12.237 W · 방향비 1.0883 | `cold2` P 10.002 W · 방향비 0.8483 |
| `coldp` 총P 22.59 W · 좌우비 1.2235 · 전/후 **1.1935** | |

**판정 기준**: 08-27 같은 날 안의 재실행이 절대 전력에서 **부호가 갈렸다**(단독 +0.8~1.6%,
짝 −2.1%). 하루를 건너서도 그 수준이면 **절대 전력 지표는 n=1 로 쓰지 않는다**가 확정된다.
비 지표(전/후, 좌우비, 방향비)는 재현성이 이미 확인됐다.

---

## 4. 제조사 표 1 회 — 약 2 시간, 2.6 Ah

**08-26 인자 그대로다.** marks 로그에서 역산해 확인했다: 전 단계 `--cycles 1 --rest 15
--rest-every 300 --dir-order cw --vmin 22.5`, `--zero-sec` 는 기본 20.

```bash
python3 test/breakin.py --tag bi1b --cycles 1 --speeds 500  --dwell 600 \
    --rest 15 --rest-every 300 --dir-order cw --vmin 22.5     # 22 분
python3 test/breakin.py --tag bi2b --cycles 1 --speeds 1000 --dwell 600 \
    --rest 15 --rest-every 300 --dir-order cw --vmin 22.5     # 22 분
python3 test/breakin.py --tag bi3b --cycles 1 --speeds 2000 --dwell 900 \
    --rest 15 --rest-every 300 --dir-order cw --vmin 22.5     # 32 분
python3 test/breakin.py --tag bi4b --cycles 1 --speeds 3000 --dwell 1200 \
    --rest 15 --rest-every 300 --dir-order cw --vmin 22.5     # 43 분
```

| 단계 | 속도 | 구성 | 08-26 대조 |
|---|---:|---|---|
| `bi1b` | 500 | CW 300 s ×2 + CCW 300 s ×2 | `bi1` 완주 |
| `bi2b` | 1000 | 〃 | `bi2` 완주 |
| `bi3b` | 2000 | 각 방향 300 s ×3 | `bi3` 완주 |
| `bi4b` | 3000 | 각 방향 300 s ×4 | ⚠ `bi4` 는 **CW 만** — 이번엔 양방향 완주 |

**끝나면 누계 CW 110 / CCW 90.** 나머지 20 분은 §6 이다.

- ⚠ **3000 rpm 은 감속기 출력 100 rpm 이다.** 베이스를 확실히 받치고 **무인 방치하지 말 것**
- ⚠ 08-26 `bi4` 는 저전압으로 CW 절반에서 끊겼다. 오늘은 28.5 V 에서 시작하므로 안 걸린다 —
  `bi4b` 진입 시 예상 26.6 V, `--vmin 22.5` 와 4 V 여유
- **무엇을 보나**: 각 단계 첫 5 분 토막의 계단. 08-26 은 냉간 CW 에서 **−6~7%**, 그 뒤
  −0.7~2.0% 였다(조치 #23). 90 분을 이미 먹은 감속기에서 그 첫 계단이 **얼마나 작아졌는지**가
  수렴 판정(#32)의 직접 지표다

---

## 5. 사후 기준선 — 15 분, 0.10 Ah

⚠ **`bi4b` 종료 20 분 안에 시작할 것.** 08-21 §14.3 의 교훈이다 — 한 시간 떨어진 두 측정을
비교하면 안 된다. 08-26 도 이 창 안에서 `post*` 를 잡았다.

```bash
python3 test/breakin.py --tag post1b --id 1 --cycles 1 --speeds 300,600,900,1500 \
    --dwell 15 --rest 10 --zero-sec 20 --vmin 22.5
python3 test/breakin.py --tag post2b --id 2 --cycles 1 --speeds 300,600,900,1500 \
    --dwell 15 --rest 10 --zero-sec 20 --vmin 22.5
python3 test/breakin.py --tag postpb --mirror --cycles 2 --speeds 300,600,900,1500 \
    --dwell 15 --rest 10 --zero-sec 20 --vmin 22.5
```

**이걸로 2×2 가 하나 더 생긴다.** 08-27 이 세운 열 × 리그 격자에 "브레이크인 90 분 vs 200 분"
축이 붙는다:

| 전/후 비 | 냉간 | 온간 |
|---|---:|---:|
| 08-26 (90 분 전) | `basep` 1.2024 | `postp` 1.1718 |
| 08-27 (90 분 후) | `coldp` 1.1935 | `warmp` 1.1623 |
| **오늘 (200 분 후)** | `coldpb` **?** | `postpb` **?** |

⚠ **`postpb` 는 온간 열에 넣어야 한다** — `warmp`/`postp` 와 짝이지 `coldp` 와 짝이 아니다.
08-27 §3 이 보인 대로 열만으로 −2.6% 가 움직인다.

---

## 6. 방향 빚 — 3000 rpm CCW 20 분, 22 분, 0.70 Ah

**목적**: 08-26 `bi4` 가 CW 절반에서 저전압 중단돼 **CW 55 / CCW 35** 로 끝났다. 여기까지
오면 CW 110 / CCW 90 이므로 **CCW 20 분**이 정확히 남는다.

```bash
python3 test/breakin.py --tag bi4ccw --cycles 1 --speeds 3000 --dwell 1200 --one-way \
    --dir-order ccw --rest 15 --rest-every 300 --vmin 22.5
```

- `--one-way --dir-order ccw` → `+3000` 만 돈다
- 끝나면 누계 **CW 110 / CCW 110** — 균형 회복, 제조사 표 2 회 완주
- ⚠ 이 런은 **온간에서 시작한다** (§4·§5 직후). 08-26 `bi4` CW 의 냉간 첫 토막과 직접
  비교하면 안 된다 — 대조 상대는 `bi4b` 의 CCW 절반이다
- ⚠ **무인 방치 금지.** 3000 rpm

---

## 7. ★ 전류 센서 직렬 DMM 교정 (조치 #4) — 45 분, 0.30 Ah

08-09 부터 열려 있는 본 시험이다. 08-27 결과가 긴급도를 크게 올렸다.

### 7.1 왜 지금 급한가

참 0 A 기준으로 잰 대기전류가 **문서값보다 47% 크다**:

```
측정   gp28 113.4 mA / gp27 110.1 mA   (08-14 게인 환산)
문서   DMM 0.077 A · 펌웨어 6.4 LSB
```

드리프트로는 설명 안 된다(두 캡처 3.6 분 간격). 남은 것은 **게인이 틀렸거나 대기전류가
변했거나**이고, 이 교정이 그것을 가른다.

동시에 게인**비**는 세 번째 독립 증거를 얻었다 — `9.4438/9.4265 = 1.0018` 이 08-21 스왑
시험의 `1.0022` 와 0.04% 로 만난다. 08-14 의 `1.0322` 는 사실상 기각이다.

### 7.2 ⚠ 먼저 DMM 레인지를 고른다 — 08-14 와 다르다

**08-14 교정은 바퀴가 없었다.** 지금은 바퀴·벨트가 붙었고 3000 rpm 까지 올린다:

| 리그 | 최대 전류 |
|---|---:|
| 바퀴 없음 (08-14 당시) | 약 0.5 A |
| 바퀴·벨트, 1500 rpm | 약 0.9 A |
| **바퀴·벨트, 3000 rpm** | **약 1.1~1.4 A** |

08-26 `bi4` 로그 실측이다 (채널당 1.319 / 1.355 A, 23.1 V). 28.5 V 에서는 1.1 A 대다.

- ⚠ **2 A 이상 레인지.** **mA 잭에 꽂으면 퓨즈가 나간다**
- 그 안에서 자릿수가 가장 많은 레인지를 쓰고, **읽은 자릿수를 그대로** 입력할 것
- **부수 소득**: raw 스팬이 08-14 의 14 LSB 에서 **110 LSB 이상**으로 는다. 08-14 게인
  정밀도를 지배하던 제약이 이번엔 사라진다

### 7.3 ⚠ DMM 을 꽂자마자 정지 1 점 (조치 #45)

**모터를 돌리기 전에**, DMM 이 직렬로 들어간 상태에서 **정지 전류를 한 점 읽는다.**
이것만으로 §7.1 의 두 가설 중 "대기전류가 실제로 변했다" 쪽이 닫힌다. **채널마다 한 번씩.**

### 7.4 절차

```bash
# 교정 직전 — 스위치 OFF 영점
python3 test/volt_compare.py --sec 60 --rate 50 -o test/logs/zero_off_pre_0828.csv

# 채널 1 — DMM 을 id=1 급전선에 직렬로  (⚠ 먼저 §7.3 정지 1 점)
python3 test/current_calib.py --id 1 --rpms 300,600,900,1500,2000,2500,3000 \
    --dwell 12 -o test/logs/current_calib_id1_0828.csv

# DMM 을 id=2 급전선으로 옮긴 뒤  (⚠ 여기서도 §7.3 정지 1 점)
python3 test/current_calib.py --id 2 --rpms 300,600,900,1500,2000,2500,3000 \
    --dwell 12 -o test/logs/current_calib_id2_0828.csv

# 교정 직후 — 스위치 OFF 영점
python3 test/volt_compare.py --sec 60 --rate 50 -o test/logs/zero_off_post_0828.csv

python3 test/calib_compare.py --tags 0814,0815,0828      # 세션 대조
```

채널당 약 15 분(운전자 입력이 지배), 채널당 0.10~0.15 Ah.

⚠ **영점 캡처는 교정과 같은 정도로 예열된 뒤에 잡을 것** — 조치 #46 이 `Z` 경로와 스트림
경로에서 3 LSB(36 mA) 차를 보였고, 08-11 의 예열 이동(5 raw ≈ 49 mA)과 크기가 맞는다.
§6 직후라 리그는 충분히 예열돼 있다.

### 7.5 ⚠ 주의

- **같은 DMM·같은 레인지로 두 채널을 같은 세션에.** 계통 게인 오차가 두 채널에 공통이라
  **비에서 상쇄된다** — 좌우비를 믿을 수 있게 만드는 조건이다
- **부담 전압(burden voltage)** 이 급전선에 직렬 저항으로 얹힌다. 양 채널 같은 조건이라
  비에는 영향 없다. 1.3 A 에서는 무시 못 할 크기일 수 있으니 레인지 선택에 반영할 것
- ⚠ **급전선을 끊는 작업 = 배선 변경.** 끝난 뒤 **핀 35(VREF)와 핀 33(Δ)을 함께 다시 재고
  기록할 것** (조치 #40). 안 재면 분압 상수가 그 시점부터 미확정이 된다
- ⚠ **Δ 는 버스가 살아 있고 모터가 정지한 상태에서 잰다.** 스위치 OFF 로 재면 안 된다
- ⚠ **모터가 돈다.** 3000 rpm 까지 올라가므로 띄운 상태 유지

### 7.6 무엇이 닫히나

| 조치 | 결과 |
|---|---|
| **4** | 절대 교정 — 종결 |
| **45** | 대기전류 110 mA vs 문서 77 mA — §7.3 이 가른다 |
| **36** | 게인비 재확정 — 1.0018 을 절대 교정이 확인 |
| **37** | 좌우비 — 절대 기준이 생겨 닫힌다. 08-27 의 1.224 는 −2.9% 여지가 있다 |
| **35** | GP27 부호 — 교정이 부호까지 확정. 이후 Python·C++·펌웨어 동시 수정 |
| **24** | `SENSOR_VZERO` — 참 0 A 기준으로 재확정 (08-26 의 1.634 V 는 대기전류 포함) |
| **27** | 영점 이동 — 전후 캡처로 점 하나 더 |

---

## 8. 배터리 예산

| 단계 | 소모 | 종료 시 전압 |
|---|---:|---:|
| 시작 | | **28.567 V** |
| §2 영점 쌍 | ~0 | 28.57 |
| §3 냉간 기준선 | 0.10 Ah | 28.50 |
| §4 `bi1b` 500 | 0.16 | 28.38 |
| §4 `bi2b` 1000 | 0.29 | 28.17 |
| §4 `bi3b` 2000 | 0.76 | 27.62 |
| §4 `bi4b` 3000 | 1.40 | 26.61 |
| §5 사후 기준선 | 0.10 | 26.54 |
| §6 CCW 20 분 | 0.70 | 26.04 |
| §7 교정 (양 채널) | 0.30 | **25.82 V** |
| **합계** | **약 3.8 Ah** | |

08-26 실측 Ah 를 전압비로 환산했다(같은 마찰이면 전류는 전압에 반비례). 평탄 구간
0.72 V/Ah — 08-27 이 0.10 Ah 에서 0.74 V/Ah 로 재확인했다.

**무릎(약 23.5 V)에 닿지 않는다.** `--vmin 22.5` 는 전 구간 유지하되 걸릴 일이 없다.
소요 시간 **약 3 시간 40 분.**

---

## 9. 이번에 하지 않는 것

| # | 항목 | 이유 |
|---|---|---|
| 39 | 감속기 교차 | 불필요 판정 유지 — 교차하면 브레이크인이 리셋된다 |
| 38 | 모터 교차 | 발동 조건 미달. ⚠ 좌우비가 08-27 에 1.224 로 벌어졌으니 §7 게인 보정 후 재확인 |
| 41 | 펌웨어 상수 갱신 | 전압 환산에 절편 항이 없어 설계 판단 필요. #4 가 끝난 뒤 일괄 |
| 8 | 접지 상태 시험 | 운전자가 띄운 상태로 결정 |
| 42 | 낡은 상수·경고 정리 | 하드웨어 미접촉. 시간 남으면 §10 |

---

## 10. 하드웨어 미접촉 — 시간 남으면

1. **속도별 분해** — §3~§6 은 사이클 집계값이다. 08-26 §6.1~6.3 의 300/600/900/1500 표를
   `cold*b`/`post*b` 로 다시 만들면 **열 효과가 저속에 몰려 있는지** 확인된다.
   `breakin_pico_*` + `breakin_marks_*` 를 구간별로 결합하면 된다.
2. **조치 #42 — 낡은 상수·경고 정리**
   - `test/breakin.py:35, :637` — *"조치 #29(접지 오프셋)가 끝나기 전에는 분압비 점으로 쓰지
     말 것"*. **#29 는 08-21·08-26 에 종결**됐고, 그 직접 측정이 분압비를 닫은 근거였다
   - `test/preflight.py` `REST_REF` — 8/11·8/12 값. 08-27 §4 가 그 간극을 **계단**으로
     규정했다. 참 0 A 기준이 생겼으니 갱신 가능하나, #27 의 서술을 "지속 드리프트"에서
     바꾸는 판단이 함께 필요하다
3. **`--reanalyze` 재현성** — 라이브 실행과 0.2% 다른 값을 낸다 (`cold1`: P 12.2105 → 12.2374).

---

## 11. 세션 후

1. 결과를 `docs/hardware_test_20260828.md` (+ 계측 분량이 크면 `_sensing` 분리) 로
2. #4 가 닫히면 **펌웨어 상수 일괄 갱신**을 별도 작업으로 (#24·#28·#35·#41 포함)
3. 이 문서를 그다음 세션용으로 다시 쓸 것
4. ⚠ **리그를 건드렸으면 날짜와 내용을 남길 것** (조치 #44) — 08-27 의 id1 벨트 텐션 변경이
   시점 미기록이라 08-26 대비 id1 비교가 통째로 교락됐다. **§7 의 급전선 재배선도 해당된다**

---

## 부록 — 08-27 에서 넘어온 값

| | |
|---|---|
| 분압 | `V_bus = (raw + 19.60) × 8.9160 mV`, `D = 11.131`, Δ = 15.7 mV (파이·확정배선) |
| | 08-27 확인: DMM 28.59 V → `D ∈ [11.128, 11.133]` (교정 범위보다 4 V 위) |
| VREF | 3.280 V (핀 35 실측) |
| **참 0 A** | **gp27 2033.486 · gp28 2031.270** (08-27 03:38, 냉간, 스위치 OFF) |
| **대기전류 스텝** | **+9.4438 / +9.4265 LSB** → 110 / 113 mA ⚠ 문서 77 mA 와 47% 차 (#45) |
| **게인비** | **1.0018** (08-27) · 1.0022 (08-21 스왑) ⚠ 08-14 의 1.0322 는 기각 |
| VZERO | 1.634 V ⚠ 대기전류 포함. 참 0 A 기준으로 재계산 필요 (#24) |
| 내부저항 | 0.070 Ω |
| 방전 기울기 | 0.72 V/Ah (평탄) / 0.89 V/Ah (무릎) |
| **전/후 (냉간)** | **1.1935** — 전진이 약 19% 비싸다. ⚠ 브레이크인 90 분의 순효과는 −0.8% 뿐 |
| **전/후 (온간)** | **1.1623** — 열만으로 −2.6%. 08-26 의 "브레이크인 −2.6%" 는 오귀속이었다 |
| **좌우비** | **1.2235**(냉간) / 1.2341(온간) ⚠ 게인 편향 −2.9% 여지 + 텐션 교락 |
| 방향비 (집계) | id1 1.0883 / id2 0.8483 ⚠ id1 은 텐션 교락 |
| 3000 rpm 채널당 전류 | **1.319 / 1.355 A** (08-26 `bi4`, 23.1 V) — §7.2 DMM 레인지 근거 |
| ⚠ 절대 전력 | **런 간 ±2%.** 4% 이하 차이는 n=1 로 판정 금지 (#43) |
| ⚠ `warm*` 태그 | **온간이다.** 냉간 기준으로 쓰지 말 것 |
